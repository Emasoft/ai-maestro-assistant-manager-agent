{
  "description": "Assistant Manager hooks - session memory persistence and user communication",
  "_note": "Hook data is passed via stdin as JSON. Scripts read from stdin, not env vars.",
  "hooks": {
    "SessionStart": [
      {
        "_id": "amama-memory-load",
        "_description": "Load session memory and context at startup",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/scripts/amama_session_start.py"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "_id": "amama-record-user-input",
        "_description": "Records the user's last-input timestamp on every prompt through the FROZEN CLI (`aimaestro-agent.sh session user-input`) — never a direct HTTP call to the server (R23). The CLI resolves auth itself from $AID_AUTH. This feeds AI Maestro's user-presence data so amama-presence-tracker can compute idle time. Fail-soft: never blocks prompt submission.",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/scripts/amama_user_prompt_submit.py"
          }
        ]
      }
    ],
    "Stop": [
      {
        "_id": "amama-stop-check",
        "_description": "Blocks exit until all coordination work is complete. Reads the inbox through the FROZEN CLI (`amp-inbox`) and GitHub through `gh` — never a direct HTTP call to the ai-maestro server (R23), which binds hooks exactly as it binds skills. A hook runs with NO skill loaded, so 'no skill instructed it' is structurally true here rather than an oversight, which is why the prohibition is restated on the hook itself.",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/scripts/amama_stop_check.py"
          }
        ]
      }
    ]
  }
}

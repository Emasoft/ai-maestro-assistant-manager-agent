---
trdd-id: 4c388042-8eac-4bf4-9679-b6deac8fe460
title: AMAMA plugin self-audit — identify errors and potential issues
column: dev
created: 2026-06-21T22:04:54+0200
updated: 2026-06-22T00:25:22+0200
current-owner: amama
assignee: amama
priority: 2
severity: MEDIUM
effort: M
task-type: audit
parent-trdd: null
relevant-rules: []
release-via: none
review-requirements: [human-review]
impacts: []
external-refs: []
---

# TRDD-4c388042 — AMAMA plugin self-audit

## ⏵ STATE — READ THIS FIRST — 2026-06-21

MAESTRO directive (2026-06-21, during the external-wait while the janitor improves
the memory system + CPV improves its upgrade/fix agents): "do an audit of the
plugin to identify errors and potential issues." Scope = the AMAMA plugin's own
source (fully in-control).

**Mechanical layer: ✅ CLEAN** — `ruff check` 0 findings; `mypy` no issues (13
scripts + 12 tests).

**Semantic audit: ✅ COMPLETE** — 5 parallel Opus agents (the first dispatch +
2 retries all hit the intermittent Anthropic throttle; MAESTRO ruling: Opus only
for delicate work, just retry — never downgrade to the external LLM). 65 findings;
5 reports in `reports/plugin-audit/` (gitignored). Mechanical layer (ruff+mypy)
already CLEAN. TEST-SUITE VERDICT: genuinely real / no-mock — every test
exercises real code (0 fake, 0 mocked-under-test); only coverage gaps.

Counts: scripts-A 18 (1 CRIT, 4 HIGH) · scripts-B 13 (0 CRIT, 2 HIGH) · tests
17 (0 high; 22 coverage gaps) · skills 9 (2 HIGH) · meta 8 (1 HIGH).

**VERIFIED against live source (the 3 disputed/top findings):**
- ✓ **C1 CONFIRMED** — `approve_plan.py:44/69` writes `.claude/orchestrator-{plan,
  exec}-phase.local.md`; `orchestration_status.py:50` reads them (OK); but
  `notify_agent.py:25` + `planning_status.py:26` read `design/{exec,plan}-phase.
  local.md` (WRONG dir+name) → `/notify-agent` + `/planning-status` ALWAYS report
  "not in phase". Real end-to-end bug.
- ✓ **`design/reports/` NOT-IGNORED** (only `/reports/` is) → `ReportWriter` +
  `stop_check` write committable reports = data-hygiene leak (F1/F2/M1).
- ✓ **`aimaestro-teams.sh tasks` verb IS deployed** (line 344 `cmd_tasks`) → the
  status-reporting skill's `DECOUPLE-BLOCKED #36 "not yet deployed"` markers are
  STALE (skills F1). Memory `amama-12` was outdated; the verb shipped. Fixable now.

**FIX PLAN (phased ≤5 files; in-control AMAMA source ONLY; commits, NO push):**
- **A. FIX-NOW (verified genuine):** C1 path drift → centralize `PLAN_STATE_FILE`/
  `EXEC_STATE_FILE` as ONE shared constant, all 4 scripts import (one-source-of-
  truth) · reports → gitignored `reports/<component>/` (report_writer + stop_check)
  · `amama-memory-recall`→`/janitor-memory-recall` (session_start docstrings) ·
  RULE-1 status≠work-order line (status-reporting + user-communication skills) ·
  teams `tasks` verb (status-reporting 5 files, drop stale #36) · H3 re-download
  crash (drop `set_readonly(folder)`).
- **B. FAIL-FAST (project mandate):** H4 swallowed download errors · M1 /tmp
  fallback · M7 notify broad-except · F6 unguarded reads · F7 non-atomic move ·
  M4 `atomic_write` helper.
- **C. JUDGMENT — surface, don't auto-fix:** F1 agents pin `model: opus` (MAESTRO
  just ruled "only opus" → KEEP per preference; read-only report-gen the only
  debatable one) · skills-F2 R32-vs-deployed-CLI `--password` contradiction ·
  skills-F8 `amama-session-memory` CozoDB vs janitor 3-scope · H1/H2 approve_plan
  stub completion (a FEATURE gap, not just a bug).
- **D. ROUTE TO CPV (publish.py canon-template, not AMAMA-authored):** F3/F4/F5/
  F9/F13 → Method-1 issue / fold into the #44/#22 CPV wave.
- **E. COVERAGE (22 gaps, mostly design_search + download CLI):** follow-on TRDD,
  python-test-writer agents.
- **F. LOW/NIT:** M2/M3/M5/M6/L1-L6/F8/F10/F11/F12 + meta-F3/F4 — batch the cheap
  genuine ones; defer cosmetic.

**Memory UPDATED** (`amama-12-decoupling-residuals-blocked-on-verbs` `[^3]`): the team-tasks
verb exists ONLY on the unmerged `governance-rules` branch (origin/main `cmd_tasks`=0) + the
deployed `~/.local/bin` lacks it — callable NOWHERE, so the DECOUPLE-BLOCKED markers STAY.
Lesson: judge verb-existence from **origin/main** (NOT the working-tree feature branch);
RECALL the governing memo before acting on a source-only audit finding. Other 'blocked'
verbs (presence, reassign-cos) likely also only-on-governance-rules — re-verify vs
origin/main when that branch merges (task #12).

**PROGRESS:**
- ✅ **A.1 — C1 FIXED + verified** (commit `b04ab8a`): `scripts/amama_state_paths.py`
  is the single source of truth (`project_root`/`plan_state_path`/`exec_state_path`);
  all 4 scripts unified; `+tests/test_amama_state_paths.py` C1 regression (writer↔readers
  agree end-to-end) + 2 readers' tests updated/de-staled. Full suite **70 passed**;
  ruff clean; mypy clean (CI flag `--ignore-missing-imports`). (The bare `uv run mypy`
  from the parent cwd reports 4 yaml/gitignore_filter stub FALSE errors — `[tool.mypy]
  ignore_missing_imports=true` + the CI flag suppress them; not a regression.)
- ✅ **A.2 — reports → gitignored** (commit `4d4cab9`): ReportWriter + Stop hook now
  write `reports/<component>/` (was committable `design/reports/`); dropped the silent
  /tmp fallback (M1 fail-fast); +`%z` timestamps (F8). 4 tests repointed; full suite
  **70 passed**, ruff + mypy clean. (Covers F1/F2/M1/F8.)
- ✅ **A.3 + A.6** (commit `2b583e4`): A.3 stale `amama-memory-recall`→`/janitor-memory-recall`
  docstrings (meta-F2); A.6/H3 download_document no longer locks the task FOLDER read-only
  (only the file + metadata) so a 2nd sibling download no longer crashes — +regression test.
  Full suite **71 passed**, ruff + mypy clean.
- ✅ **A.4 — RULE-1 guard** (commit `0745691`, KEPT): added a one-line RULE-1
  (status≠work-order) guard to the status-reporting + user-communication skills and the
  role-routing "Status requests" branch [skills-F5]. Verified present in all 3.
- ⚠️ **A.5 — REVERTED** (applied `0745691`, reverted `51fc872`): repointing the team-tasks
  refs to `aimaestro-teams.sh tasks` was PREMATURE. RECHECK vs memory
  [[amama-12-decoupling-residuals-blocked-on-verbs]] + verification: the verb exists ONLY
  on the unmerged `governance-rules` branch (origin/main `cmd_tasks` count = 0) and the
  deployed `~/.local/bin` copy lacks it — callable NOWHERE. Restored the DECOUPLE-BLOCKED +
  `/api/` fallback in all 4 files (correct until the USER merges governance-rules + the CLIs
  deploy). The audit's skills-F1 was a source-only finding; I failed to RECALL the governing
  memo first (the proactive-recall contract exists for exactly this). A.5 is DEFERRED to the
  post-merge+deploy window: re-verify the DEPLOYED verb, THEN repoint.
- ✅ **Bucket B — COMPLETE (fail-fast)**: B1/M4 (commit `91a39fe`) shared `atomic_write`
  helper for the phase-state/metadata/index SoT writes (+test); B2/F6+F7 (commit `4c6fa28`)
  atomic `apply_move` with rollback-on-move-failure + guarded 2 reads in proposal_approvals
  (+2 tests); B3/M7+H4 (commit `da8deba`) notify narrows its except + surfaces the cause to
  stderr, download main() surfaces the failure buffer to stderr (download_document's tested
  return-None contract + already-specific excepts KEPT) (+2 tests). Full suite **77 passed**,
  ruff + mypy clean.

**NEXT (genuine in-control fixes A+B all DONE):** remaining work is routing/judgment, not
in-control code:
- **C — SURFACE to MAESTRO** (4 judgment calls, awaiting USER): F1 agents pin `model: opus`
  (keep per "only opus"); skills `--password` vs R32; `amama-session-memory` CozoDB vs the
  janitor 3-scope; H1/H2 `approve_plan` stub (feature gap — complete vs make honest).
- **D ✅ ROUTED** — publish.py canon-template findings F3/F4/F5/F9/F13 (not AMAMA-authored;
  generated copy) filed as CPV#146 (distinct from the CLOSED CPV#142 CI-bug batch; verified
  no duplicate). Fix lands CPV-side + propagates on the next canon-pipeline upgrade.
- **E ✅ DONE (HIGH-value)** — TRDD-0f491ba0: +15 real no-mock tests (design_search
  CLI/search +10, download lookup/extract/init/CLI +5), suite 78→93, scripts unchanged.
  LOW/NIT gaps deferred there (stop_check error branches, write_*_file False-return,
  proposal render helpers, thin entry wrappers).
- **F — low/nit** (M5 ✅ dead `set_readonly` dir branch removed; M2 ✅ `--task-id`
  parse-time allowlist validator). Clean F items DONE; remaining DEFERRED (judgment/risk,
  NOT clean autonomous fixes):
  - **L1** `shared/thresholds.py` is dead (imported nowhere) + partly obsolete
    (`MAX_MEMORY_ENTRIES`/`MEMORY_TTL_DAYS` configured the memory-bank retired by
    TRDD-8707e849); the rest don't map to call sites. Recommend DELETE (or wire
    `MAX_MESSAGE_LENGTH`/`VALID_GOVERNANCE_ROLES` only if enforcement is wanted). NOT
    auto-deleted — RULE 0 (committed a prior session, not this one) → surface for USER.
  - **L2** dup `parse_frontmatter` (notify/planning/proposal + design_search regexes):
    DRY consolidation deferred (multi-file; variants must be diffed first to avoid drift).
  - **M6** dead `--skip-issues` flag: tied to the `approve_plan` stub (bucket-C H1/H2).
- **DEFERRED A.5 + FOLLOW-UP (task #12)**: when `governance-rules` merges to origin/main +
  the CLIs deploy, re-verify EACH blocked verb (team-tasks, presence, reassign-cos) in the
  DEPLOYED CLI, then repoint. RECALL [[amama-12-decoupling-residuals-blocked-on-verbs]] first.

## Skills + meta findings — disposition (completeness check, 2026-06-22)

Cross-checked all 17 skills+meta findings (the script buckets A–F covered the scripts
reports; this accounts for the rest so nothing is silently dropped):
- **Done:** meta-F2 (`amama-memory-recall` ref → `/janitor-memory-recall`) = A.3; skills-F5
  (RULE 1 status≠work-order) = A.4.
- **Surfaced to USER (bucket C):** skills-F8 (CozoDB `amama-session-memory`), skills-F2
  (`--password` vs R32), meta-F1 (`model: opus` pins — keep per "only Opus").
- **A.5-deferred (verb undeployed):** skills-F1 (team-tasks), skills-F3 (presence).
- **Routed to owning tasks:** skills-F4 (markers cite CLOSED #36; residuals are #45) → task
  #12; skills-F7 ("5-Status Model" vs the server's 17-column kanban) → task #11.
- **Clean LOW — DONE (commit `d09c486`):** skills-F6 ✅ tier-ladder cross-refs added to
  amama-approval-workflows + amama-autonomous-fallback; meta-F3 ✅ MCP refs softened to
  drift-proof globs (`mcp__*serena*` / `mcp__*llm-externalizer*`, resolved via ToolSearch).
- **Surfaced as a design decision (bucket-C-style, USER decides):** meta-F7 — the Stop hook
  blocks exit on any open `assistant-manager` issue regardless of session. Gating on
  `$AID_AUTH` would fix the (low-prob) label-collision wedge but DISABLES the block for a solo
  AMAMA user running without the AI-Maestro server. Behavior change + trade-off; audit rates
  it "None mandatory" → not auto-changed.
- **Acceptable / cosmetic:** meta-F6 (amp-inbox fail-soft already handled), skills-F9 +
  meta-F4 (NIT — description/changelog style).

## Why

Productive in-control work during the fleet external-wait; keeps the MANAGER plugin
clean by construction.

## Notes and lessons learned

(none yet)
